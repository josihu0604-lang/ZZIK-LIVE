/**
 * Mock for server-only package in test environment
 * Allows testing of server-side code without restrictions
 */

// Export nothing - this module is just to prevent import errors in tests
export {};
